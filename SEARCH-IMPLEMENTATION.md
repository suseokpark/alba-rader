# 알바레이더 동시 검색

## 실행

```sh
npm install
npm run dev -- --host 127.0.0.1
```

기본 검색 범위는 **주소 기준**, 지역 단위는 **시·군·구(`district`)**다. 업체의 선택 상태는 세 업체 모두로 시작하지만, 이 기본 단위에서 실제 조회 가능한 업체는 알바몬·알바천국이다. **주소 찾기**에서 기준 주소를 선택한 뒤 지역 단위와 검색어를 지정하고 검색한다. 업체 선택이나 지역 단위 변경은 다음 검색에 반영된다. 빠른 검색 버튼과 Enter도 같은 검색을 실행한다.

지역 단위는 동·읍·면(`neighborhood`), 시·군·구(`district`), 시·도 전체(`province`)를 제공한다. 수원시 영통구처럼 구가 있는 일반시의 주소에는 별도의 시·군 전체(`city`) 선택지도 표시한다. 서울 마포구에서 넓히면 서울 전체(`province`), 경기 수원시 영통구에서는 수원시 전체(`city`)와 경기 전체(`province`)를 선택한다. 군 지역이나 구가 없는 일반시처럼 기존 시·군·구와 범위가 같으면 중복된 시·군 전체 선택지는 표시하지 않는다. 세종처럼 `sigungu`가 없는 주소를 선택하면 `district`를 `province`로 자동 변경한다. 주소 변경으로 기존 `city` 선택지가 없어지면 해당 주소의 유효한 시·군·구 또는 시·도 단위로 정규화한다.

당근의 기본 단일 동네 모드는 **주소 기준 + 동·읍·면**일 때만 활성화된다. **당근 여러 동네 선택**을 켜면 기준 주소의 시·군·구·시 전체·시·도 전체·전국 설정과 별개로 당근 동네 목록을 검색할 수 있다. **전국**을 선택하면 알바몬·알바천국은 주소 없이 전국을 검색하고 지역 단위 선택창은 비활성화된다. 이때 여러 동네 모드의 당근은 별도로 선택한 동네들만 조회하며, 당근 전국 검색으로 해석하지 않는다.

## 당근 여러 동네 선택

- 체크박스는 기본으로 꺼져 있다. 빈 목록에서 켤 때 현재 기준 주소가 있으면 그 주소의 동네를 초기 추가한다. 여러 동네 모드에서 당근을 검색하려면 최소 한 곳을 선택해야 한다.
- **동네 추가**의 다음·카카오 우편번호 검색은 기존 기준 주소를 바꾸지 않는다. 선택 결과 중 `SearchArea`의 `sido`, `sigungu`, `bname`, `bcode`, `sigunguCode`만 별도 목록에 보관하며 도로명 주소·우편번호는 목록에 저장하지 않는다. 현재 기준 주소의 동네를 직접 추가하는 버튼도 제공한다.
- 최대 5개의 고유 동·읍·면을 선택한다. 정규화한 시도·시군구·동읍면 이름으로 같은 동네의 중복 추가를 막는다. 같은 읍·면에 속한 서로 다른 리 주소도 같은 선택 동네로 취급한다.
- 동네 목록은 기준 주소와 지역 단위 변경에 영향받지 않는다. 체크를 끄면 기존 단일 동네 동작으로 돌아가고 별도 목록은 보존한다. 다시 켜면 해당 목록을 사용한다. 영구 저장은 하지 않으므로 새로고침하면 목록과 체크 상태가 초기화된다. 변경 사항은 다음 검색에 적용된다.
- 각 동네에 기존 `/api/search`를 `source=daangn&scope=address&areaLevel=neighborhood`로 요청한다. 동네당 최대 20건, 당근 동네 요청은 최대 2개를 동시에 실행한다. 여러 동네 결과는 선택한 동네 순서와 각 원문의 공고 순서를 기준으로 합치고 공고 ID 또는 정규화한 원문 URL이 같은 중복을 제거한 후 필터·정렬을 적용한다.
- 당근 결과에는 동네별 성공·0건·실패 상태, 조회 건수, 원문 검색 링크와 합치는 과정에서 제거한 중복 수를 표시한다. 응답 완료 순서가 달라도 기본 공고 순서는 선택 동네 순서를 유지한다.
- 일부 동네가 실패하면 나머지 동네의 결과만 표시하고 일부 실패임을 알린다. 성공한 동네가 모두 0건이어도 실패한 동네의 공고 유무는 미확인으로 표시한다. 여러 동네를 합친 결과는 해당 동네와 주변의 일부 공고이며 구·시·도 전체나 전국 공고가 아니다.

**현재 주소 찾기 문제는 해결 완료가 아니다.** 기존 내장 브라우저 탭에서는 주소 선택·변경과 공고 검색이 성공했지만, 새 탭에서는 공식 주소 검색 프레임이 차단되어 새 탭 사용 검증에 실패했다. 정확한 차단 원인은 미확정이다. 일반 Chrome에서의 정상 동작도 별도로 확인했으나, 이를 내장 브라우저 문제 해결의 근거로 삼지 않는다.

## 주소 선택과 API

- 주소 찾기는 다음·카카오 공식 우편번호 SDK `https://t1.kakaocdn.net/mapjsapi/bundle/postcode/prod/postcode.v2.js`를 로드해 앱의 대화상자 안에 `Postcode.embed()`로 표시한다. `kakao.Postcode`와 `daum.Postcode`를 지원한다.
- SDK의 공식 옵션 `submitMode: false`와 `embed(container, { autoClose: false })`를 사용한다. 주소 선택 후 모달 닫기와 iframe 컨테이너 정리는 앱이 담당한다.
- `createPostcodeSearch()`가 SDK 로딩과 주소 검색 프레임의 준비 상태를 구분한다. 스크립트 `onload`만으로 준비 완료를 표시하지 않고, 실제 프레임이 보낸 유효한 크기의 `onresize` 콜백을 받은 뒤 `ready`로 전환한다. 준비 신호가 8초 동안 없으면 프레임을 한 번 다시 생성하고, 두 번째 시도도 8초 동안 무응답이면 오류와 재시도 안내를 표시한다. SDK 스크립트 자체의 로딩 제한은 별도로 12초다.
- 열기 세대 번호와 프레임 시도 번호로 이전 프레임의 지연 콜백을 무시한다. SDK 로딩 중 닫고 다시 열어도 최신 요청만 프레임을 생성하며, 닫을 때 재시도 타이머와 프레임을 정리한다. 팝업 대체 경로는 현재 구현에 포함하지 않는다.
- `src/app.html`의 CSP 메타 태그 `upgrade-insecure-requests`는 로컬 HTTP 페이지의 스킴을 상속한 외부 SDK 요청을 HTTPS로 올린다. 브라우저 보안 설정을 완화하거나 차단 기능을 해제하지 않는다.
- 공식 검색의 선택 완료 값으로 도로명 또는 지번 주소와 우편번호를 화면에 표시한다. 읍·면 이름 `bname1`이 있으면 이를, 그 외에는 `bname`을 검색 기준으로 사용한다. 주소 변경·지우기·불러오기 실패 시 재시도를 제공한다.
- 주소 문자열과 우편번호는 앱의 공고 검색 API에 보내지 않는다. 주소 기준 요청에는 행정구역 필드 `sido`, `sigungu`, `bname`, `bcode`, `sigunguCode`와 지역 단위 `areaLevel`을 전송한다. 주소를 검색하는 과정 자체는 외부 다음·카카오 우편번호 서비스에서 처리한다.
- 요청 형식은 `/api/search?q=...&source=daangn|albamon|alba&scope=address|nationwide`이다. 주소 기준 요청에는 위 행정구역 필드와 `areaLevel=neighborhood|district|city|province`를 추가한다. 화면은 지역 단위를 명시하며 기본값은 `district`다. 기존 호출 호환성을 위해 API에서 `areaLevel`을 생략하면 `neighborhood`를 유지한다. `scope` 생략 시 API 기본값은 `nationwide`이며, 화면 기본값 `address`와 구분한다.
- API는 검색어 1~80자, 허용 업체·범위·지역 단위, 행정구역 필드 형식 및 법정동 코드의 시군구 코드 접두사 일치를 검사한다. 빈 문자열을 포함한 잘못된 `areaLevel`과 누락·불일치 주소는 외부 조회 전에 HTTP 400으로 반환한다. 당근의 넓은 지역 범위처럼 형식은 유효하지만 해당 업체가 지원하지 않는 요청은 공고 없이 `unavailable`로 반환한다.

## 지도 보기

- 기준 주소 옆, 당근 선택 동네 칩, 당근 조회 결과의 동네별 항목에서 지도 링크를 제공한다. [카카오맵 공식 지도 URL 안내](https://apis.map.kakao.com/web/guide/#mapurl)의 검색 링크 형식인 `https://map.kakao.com/link/search/`에 `encodeURIComponent(query)`를 붙여 새 탭으로 연다.
- 기준 주소 지도는 선택 결과의 `address` 전체를, 당근 동네 지도는 `sido + sigungu + bname`만 검색어로 사용한다. 해당 링크를 클릭할 때만 주소 또는 동네 검색어를 카카오맵에 전달한다. 링크에는 `target="_blank"`, `rel="noopener noreferrer"`, `referrerpolicy="no-referrer"`를 적용한다.
- 기준 주소를 바꾸면 기준 지도 링크도 갱신하고 주소를 지우면 숨긴다. 당근 선택 목록의 지도는 현재 선택 동네를 따르며, 이미 조회된 결과의 지도 링크는 조회 당시 저장된 동네 정보를 유지한다.
- 지도 기능에 API 키·지오코딩·지도 SDK·앱 내 지도 iframe을 추가하지 않으며 좌표를 추정하지 않는다. 주소·동네의 외부 지도 검색으로, 개별 공고의 정확한 근무지나 검색 범위의 반경·행정경계를 표시하는 기능은 아니다.

## 동작

- 브라우저가 선택한 업체별 검색 API를 병렬 요청한다.
- 서버는 주소 기준인 경우 공식 공개 지역 자료로 서비스별 지역 코드를 확인하고, 해당 조건의 공개 검색 HTML을 조회한다. 전국 검색에는 지역 제한을 넣지 않는다. 팝업/외부 이동은 공고 검색 실행에 필요하지 않다.
- 응답이 온 업체부터 결과를 표시한다. 각 공고 링크는 실제 개별 공고 원문으로 연결된다.
- 업체 표시 순서는 선택 순서와 관계없이 **알바몬 → 당근 → 알바천국**이다.
- 단일 업체·지역 요청은 최대 20건이며 지역 코드 확인과 공고 조회를 합쳐 12초 제한이다. 당근 여러 동네 모드는 이 요청을 동네별로 실행하므로 합친 결과는 20건을 넘을 수 있다. 검색 결과에는 60초의 프로세스 메모리 캐시, 최대 100검색, 동일 진행 요청 병합을 적용한다. 캐시 키에 업체·검색어·범위·주소 기준의 `areaLevel`·행정구역을 모두 포함하여 동네 결과가 넓은 범위에 재사용되지 않게 한다. 실패 응답은 캐시에서 제거한다. 서비스별 지역 코드 목록은 별도 메모리 캐시를 사용한다.
- 수집 실패 `unavailable`과 원문의 명시적인 0건 `empty`를 구분한다. 테스트 데이터로 대체하지 않으며 지역 매핑이나 조건 확인에 실패하면 전국 결과로 대체하지 않는다.
- 로그인·사용자 계정·비공개 인증 정보는 사용하지 않는다.

## 범위

- 당근: 개별 요청은 `neighborhood`에서 선택한 주소의 동네와 **주변 지역**을 포함하는 공개 검색 결과다. 공식 지역 코드로 `regionId`를 지정하고 최종 URL·페이지 제목의 지역과 검색어를 확인한다. 개별 API의 `district`·`city`·`province`·전국 요청은 계속 `unavailable`로 반환한다. 여러 동네 모드는 별도로 선택한 동네마다 `neighborhood`를 요청해 합치는 기능이며, 한 동네나 여러 동네 결과를 넓은 행정구역 전체로 표시하지 않는다.
- 알바몬: `neighborhood`는 읍·면·동, `district`는 주소의 전체 시·군·구, `city`는 해당 일반시의 모든 하위 구, `province`는 시·도 전체이며 전국도 지원한다. 공식 코드 목록을 계층적으로 매핑해 `areas`를 지정하고 인근 지역 확장을 끈다. 응답의 적용 지역 코드 집합이 요청한 집합 전체와 정확히 일치하는지 확인하며, 일부 구만 적용된 결과나 요청하지 않은 지역이 추가된 결과는 사용하지 않는다. `SEARCH_RECRUIT_LIST` 일반 목록과 실제 상세 링크를 검증한다.
- 알바천국: `neighborhood`는 동 단위 검색을 지원하지 않아 **주소가 속한 시·군·구의 더 넓은 범위**로 검색하고 이를 명시한다. `district`는 원주소의 전체 시·군·구, `city`는 해당 시의 모든 구, `province`는 공식 시·도 전체 항목을 사용하며 전국도 지원한다. 일반 공고 영역 `#jobNormal`만 읽고 광고 영역은 제외한다. 공식 `hidArea` 복수 조건 또는 전체 조건을 지정하고 응답의 지역 조건 전체가 일치하는지 확인한다. 시 전체에 필요한 구가 알바천국의 공식 선택 제한인 5개를 넘으면 일부 구로 줄이지 않고 `unavailable`과 구·군 또는 시·도 검색 안내를 반환한다. 세종은 시 전체 기준이다.
- 적용 지역과 실제 검색 단위는 업체별 `regionNote`로 화면에 표시한다. 업체마다 검색 범위가 다르며, 선택 주소와 공고 사이의 정확한 거리나 반경을 계산하지 않는다.
- 전체 공고 개수나 전체 페이지를 수집하지 않는다. 세 업체의 검색 순위/포함 기준은 서로 다를 수 있다.
- 원문 검색 구조가 변경되면 어댑터를 갱신해야 한다. 마감·지원 가능 상태는 공고 원문에서 최종 확인한다.
- 현재 로컬 SvelteKit 서버용. 외부 호스팅은 실행하지 않았다.

## 정렬

- **업체 기본순**: 각 업체가 반환한 첫 페이지의 공고 순서를 그대로 유지한다. 당근 여러 동네 모드는 선택한 동네 순서와 각 동네의 원문 순서대로 합친 뒤 중복을 제거한 순서다.
- **시급 높은순**: 업체별로 이미 불러온 공고 안에서 명시적인 원화 시급만 내림차순으로 정렬한다. 같은 시급은 원래 순서를 유지한다. 월급·일급·협의·시급 범위 등 단일 시급을 확정할 수 없는 공고는 뒤쪽에서 원래 순서를 유지한다.
- 월급이나 일급을 시급으로 환산하지 않는다. 이 정렬은 각 업체 전체 공고의 시급 순위를 의미하지 않으며, 세 업체 공고를 하나의 통합 순위로 합치지 않는다. 거리순 정렬은 구현하지 않았다.

## 결과 필터

- 필터는 검색 API에 추가 조건을 보내지 않고, **이미 불러온 업체별 첫 페이지 공고에만** 즉시 적용한다. 당근 여러 동네 모드는 각 동네의 첫 페이지를 합쳐 중복 제거한 공고에 적용한다. 업체 전체 결과의 조건 검색이나 전체 건수를 의미하지 않는다. 업체 순서(알바몬 → 당근 → 알바천국)는 유지한다.
- 급여 형태: 시급·일급·주급·월급·연봉·건당을 원문의 급여 접두사로 구분한다. 최소 시급은 확정된 표시 시급만 비교하며, 월급·일급 환산이나 협의·범위 금액 추정을 하지 않는다. 시급 외 급여 형태를 고르면 최소 시급은 초기화·비활성화한다.
- 근무요일: 평일만·주말만·요일 협의. 명시된 요일만 판별하며 `주5일`을 평일, `주2일`을 주말로 추정하지 않는다. 날짜형 단기근무도 요일로 임의 변환하지 않는다.
- 근무 시작 시간대: 00시 이상 06시 미만 / 06시 이상 12시 미만 / 12시 이상 18시 미만 / 18시 이상 24시 미만, 또는 시간 협의. 전체 근무 구간이나 일일 근로시간이 아니라 **표시된 시작 시각** 기준이다.
- 포함·제외 키워드: 제목과 회사명에서만 찾는다. 쉼표 구분, 포함은 모든 키워드(AND), 제외는 하나라도 일치(OR)다. 대소문자·전각 문자·연속 공백을 정규화하고 빈 키워드는 무시한다. 제목 키워드 매치는 실제 근무조건의 확인을 뜻하지 않는다.
- 요일·시간 정보가 없거나 모호한 공고는 해당 조건을 선택했을 때 기본적으로 제외한다. `요일·시간 미확인 공고도 포함`을 체크하면 확인이 필요한 공고를 배지로 구분해 포함한다. 확인된 불일치나 급여·키워드 불일치는 이 옵션으로 허용하지 않는다. 알바천국은 현재 목록 응답에 요일·시간이 없어 이 정책의 대상이다. 추가 상세페이지 수집은 하지 않았다.
- `불러온 N건 · 표시 M건`, 업체별 표시/조회 건수, 원문 0건과 필터 후 0건을 구분한다. 새 검색에도 필터를 유지하며, 전체 필터 초기화는 원래 불러온 공고와 정렬을 복원한다. 주소와 검색어는 초기화하지 않는다.

## 검증

`test:search`는 개발 서버를 켠 상태에서 실행한다. 기본 대상은 `http://127.0.0.1:5173`이며 `ALBA_RADAR_BASE_URL`로 바꿀 수 있다. 실조회 검사는 업체의 공개 검색에 요청하므로 외부 서비스 상태에 영향을 받는다.

```sh
npm run check
npm run build
npm run test:unit
npm run test:search
```

- `test:unit` → `scripts/search-options.test.mjs`: 업체 표시 순서, 시급 파싱·안정 정렬·급여 단위 구분, 주소 선택 콜백의 도로명/지번·읍면동 필드 매핑을 검증하는 3개 테스트다.
- 같은 명령이 실행하는 `scripts/postcode-lifecycle.test.mjs`에는 5개 테스트가 있다. SDK 로딩과 프레임 준비의 구분, 빈 프레임의 1회 재시도 후 오류, SDK 로딩 중 닫기·재열기의 단일 프레임 생성, 닫은 프레임의 지연 콜백·타이머 취소, 정상 재시도 후 주소 선택을 검증한다.
- 결과 필터 추가 시점에는 `scripts/filter-jobs.test.mjs` 7개와 `scripts/schedule-filters.test.mjs` 9개를 포함해 총 24개를 실행했다. 급여 단위·범위·누락, 키워드 AND/OR, 원순서 보존, 복합 조건, unknown 허용 범위, 시간 경계·자정 넘김·협의·요일/기간 오인 방지를 검증한다.
- `test:unit`은 `scripts/*.test.mjs`를 실행한다. 지역 범위 확장 단계에서 `search-area.test.mjs`, `albamon-area.test.mjs`, `alba-area.test.mjs`, `daangn-area.test.mjs`를 포함한 전체 57개가 통과했다. 주소에 따른 선택지·정규화, 공식 지역 집합의 완전성, 알바천국 5개 제한, 응답 조건 불일치 및 당근의 넓은 범위 차단 등을 검증한다.
- `test:search` → `scripts/smoke-search.mjs`: 서울 마포구 서교동의 세 업체 동시 API 조회, 실제 공고 반환, 공식 개별 링크, 중복 ID, 검색어 보존, 당근 `regionId=230`, 알바몬 `areas=I1300180`, 알바천국 `hidArea=02||마포구,` 적용을 검증한다. 이 동네 요청은 `areaLevel` 생략 시의 기존 동작도 확인한다. 알바몬 공고의 서교동·알바천국 공고의 마포구 표기를 확인한다.
- 같은 실조회 검사는 알바몬·알바천국의 마포구(`district`)·서울 전체(`province`)와 수원시 영통구(`district`)·수원시 전체(`city`)·경기 전체(`province`)를 조회한다. 응답 링크의 공식 지역 코드 집합이 요청 범위와 정확히 일치하는지, 수원시 전체 결과의 지역이 수원인지 검사한다.
- 전국에서는 알바몬·알바천국의 지역 제한 제거와 당근의 `unavailable` 응답을 확인한다. 당근의 `district`·`city`·`province`가 동네 결과를 재사용하지 않고 공고 없이 `unavailable`로 반환되는지, 누락·불일치 주소 및 잘못된 검색 입력·지역 단위가 HTTP 400인지도 검사한다. 외부 연결 불가 시 통과로 처리하지 않고 실패한다.

2026-09-23: 주소 기준·전국·정렬 구현 후 `test:search` 통과. 이후 주소 검색 생명주기 수정에서 기존 구현의 결함을 테스트 실패로 재현한 뒤 수정하여 통과를 확인했다. 그 수정 단계에서 `check` 오류·경고 0건, `test:unit` 전체 8개 통과, `build` 통과를 확인했다. 아래 추가 브라우저 재검증은 이 자동 검사 기록과 구분한다.

같은 날 주소 기준·전국·정렬 구현 단계의 실제 브라우저에서 아래 흐름을 확인했다. 공고 건수와 급여 값은 이 검증 시점에 불러온 첫 페이지 기준이며, 이후 새 내장 브라우저 탭에서 재현된 주소 검색 차단은 아래 후속 검증과 구분한다.

- 공식 SDK embed에서 `서울 마포구 양화로 160`을 검색하고 `서울 마포구 양화로 지하 160 (홍대입구역)` 도로명 주소를 선택했다. 앱에 우편번호 `04050`과 `서울 마포구 동교동`이 자동 반영되었다. 별도 주소 `서울 마포구 양화로6길 90`의 선택 콜백도 `합정동`으로 반영되는 것을 확인했다.
- `카페` 동시 검색에서 총 50건을 표시했다. 당근 20건은 동교동 및 주변, 알바몬 20건은 동교동, 알바천국 10건은 마포구 기준이었다.
- **시급 높은순**으로 바꾸어 각 업체 첫 3개 시급을 확인했다. 당근은 `20,000 / 13,000 / 12,000원`, 알바몬은 `12,000 / 12,000 / 11,500원`, 알바천국은 `13,200 / 12,000 / 11,000원`이었다.
- **전국** 전환 후 실제 30건(알바몬 20건·알바천국 10건)을 표시했고, 두 업체 모두 `전국 검색` 표시와 당근 비활성 상태를 확인했다.
- **주소 기준**으로 돌아가 **업체 기본순**으로 검색했을 때 세 업체 50건 표시를 다시 확인했다.
- 당시 `autoClose: false` 적용 뒤 위 흐름에서 새 오류가 발생하지 않았다. 이전 SDK `removeChild` 오류는 그 수정 전 관찰 기록이다.

### 주소 찾기 실패 후속 검증

사용자가 다시 보고한 주소 찾기 실패를 조사하면서 스크립트 로드만으로 준비 완료를 표시하던 결함과, SDK 로딩 중 닫기·재열기 시 `embed()`가 두 번 호출되는 경합을 재현했다. 위 준비 신호·재시도·세대 및 시도 번호 검사로 해당 앱 동작을 수정했다.

- 일반 Chrome에서 동일 코드의 공식 주소 검색으로 홍대입구역의 양화로 지하 160 주소를 선택했다. `04050 / 서울 마포구 동교동` 반영 후 `카페`를 검색하여 알바몬·당근·알바천국 순서로 총 50건을 확인했다.
- 내장 브라우저의 기존 탭에서는 주소 검색 화면 정상 표시를 확인했지만, 새 탭에서는 SDK 프레임의 `/search` 요청에 `net::ERR_BLOCKED_BY_CLIENT`가 계속 관찰되었다. Chrome의 정상 동작은 새 내장 브라우저 탭의 차단 해결을 의미하지 않는다.
- 공식 팝업 방식도 내장 브라우저에서 창이 생성되지 않아, 시험용 팝업 대체 코드는 제거했다. 현재 동작은 공식 embed와 오류·재시도 안내다.
- 내장 브라우저 차단의 정확한 원인은 미확정이다. HTTPS 승격이나 SDK 옵션 하나가 원인이거나 완전한 해결책이라고 단정하지 않으며, 이번 수정으로 주소 검색 문제가 모든 환경에서 완전히 해결되었다고 주장하지 않는다.

사용자의 재검증 요청 후 다음을 추가로 관찰했다. 아래 건수는 해당 시점의 `카페` 검색 첫 페이지이며, 업체 순서는 알바몬·당근·알바천국이다.

| 검증 환경·동작 | 관찰 결과 |
| --- | --- |
| 기존 내장 브라우저 탭: 공식 위젯에서 홍대입구역 주소 선택 후 검색 | 20 / 20 / 10건, 총 50건 표시 |
| 같은 탭: 벡스코 `부산 해운대구 APEC로 55`로 실제 주소 변경 후 검색 | 알바몬 우동 14건 / 당근 우동 및 주변 20건 / 알바천국 해운대구 10건, 총 44건 표시 |
| 같은 탭: 주소 검색 닫기·재열기 3회, 새로고침 후 첫 열기 3회 | 모두 주소 검색 화면 표시 성공 |
| 새 내장 브라우저 탭 6: 처음 열기 및 자동 재시도 | `/search`가 요청 후 약 1ms에 `net::ERR_BLOCKED_BY_CLIENT`, `blockedReason: other`로 종료. HTTP 응답 없음 |
| 동일 origin·CSP·SDK의 정적 최소 페이지: Svelte·대화상자·앱 재시도 코드 제외 | 동일 `/search` 차단 재현, 약 1.4ms에 종료. HTTP 응답 없음 |

기존 탭의 반복 성공과 새 탭의 실패가 함께 확인되었다. 대화상자나 앱의 8초 타임아웃만이 차단 원인이라는 설명은 위 관찰로 지지되지 않는다. `blockedReason: other` 자체로 구체적인 차단 주체나 정책을 식별할 수 없으므로 정확한 원인은 미확정으로 남긴다. **새 탭에서 주소 선택을 완료하는 검증은 실패 상태이며, 주소 찾기 문제를 해결 완료로 처리하지 않는다.**

이 재검증에서도 `check` 오류·경고 0건, `test:unit` 8개 통과, `test:search` 통과, `build` 통과를 확인했다. 자동 검사는 새 내장 브라우저의 외부 프레임 차단을 재현하는 테스트가 아니므로, 그 통과를 주소 찾기의 사용 환경 검증 통과로 간주하지 않는다. 조사용 정적 페이지와 임시 탭은 제거했고, 네트워크 관찰도 종료했다.

### 결과 필터 추가 후 검증 — 2026-09-23

자동 검사: `check` 오류·경고 0건, `test:unit` **24개 통과**, `test:search` 통과, `build` 통과. 개발 서버는 새 모듈 연결 후 `127.0.0.1:5173`에서 다시 시작했다. 아래는 실제 Chrome 새 탭에서 공식 주소 검색·선택을 거쳐 수행한 UI 검증이며, 내장 브라우저 새 탭의 기존 주소 검색 문제 해결을 의미하지 않는다.

- 홍대입구역 주소를 선택하고 `카페` 검색: 알바몬 20건 → 당근 20건 → 알바천국 10건, 불러온 50건/표시 50건.
- 최소 시급 12,000원: 표시 11건, 표시된 모든 급여가 시급 12,000원 이상임을 확인.
- 월급으로 변경: 최소 시급이 0으로 초기화되고 선택창이 비활성화됨. 표시 11건의 급여가 모두 월급임을 확인.
- 초기화: 원래 50개 공고 URL과 순서가 완전히 복원됨.
- `카페` 포함·`주말` 제외: 표시 28건. 변경 전 제목·회사명으로 독립 계산한 예상 URL 28개와 순서까지 일치.
- 주말만: 표시 8건(3/5/0). 표시된 모든 일정이 토·일만 명시함. 미확인 포함 체크 시 표시 38건, 이 중 30건에 확인 필요 배지. 알바천국도 10건 표시.
- 주말만 + 저녁 시작, 미확인 제외: `토,일 · 18:30 ~ 23:30` 1건 표시.
- 불일치 키워드: 표시 0건, 세 업체 모두 원문 검색 0건이 아닌 필터 후 0건 안내. 해당 안내의 초기화 버튼 정상 동작.
- 시급 필터와 시급 높은순 정렬을 함께 적용: 업체 순서를 유지하면서 업체별 명시 시급 내림차순 확인.
- 최소 시급을 유지한 채 전국 재검색: 알바몬·알바천국 불러온 30건/표시 6건, 최소 시급 12,000원 선택 유지 및 당근 제외 확인.
- 실제 데스크톱 Chrome 화면에서 필터 배치 확인. 모바일 브라우저 실기기 검증은 수행하지 않았다.

### 지역 범위 확장 후 자동·실조회 검증 — 2026-09-23

`check` 오류·경고 0건, `test:unit` **57개 통과**, `build` 및 `test:search` 통과를 확인했다. 동네 범위의 세 업체 검색과 기존 API 생략값 호환성, 마포구·서울 전체, 영통구·수원시 전체·경기 전체의 공식 지역 조건 적용을 검사했다. 넓은 범위의 다섯 사례는 각각 알바몬 20건·알바천국 10건을 반환했으며, 이 건수는 검사 시점에 불러온 첫 페이지 일부다. 당근의 넓은 범위 요청과 잘못된 `areaLevel`·주소 요청의 차단 검사도 통과했다.

실제 Chrome 새 탭에서 공식 주소 검색을 거쳐 다음을 확인했다. 건수는 검증 시점에 불러온 첫 페이지 일부이며 전체 모집 공고 수가 아니다.

- 경기도청 `경기 수원시 영통구 도청로 30` 선택 후 기본 범위는 시·군·구. 영통구 검색에서 알바몬 20건·알바천국 10건을 표시하고 원문 조건 `B201` / `031||수원시 영통구,`를 확인했다.
- 시·군 전체로 전환 후 수원시 30건을 표시했다. 원문 조건에 권선·영통·장안·팔달 네 구가 모두 포함됐으며, 표시된 공고의 지역이 모두 수원이었다.
- 최소 시급 12,000원을 적용하면 수원시 결과는 불러온 30건/표시 1건. 경기 전체로 재검색한 뒤에도 필터가 유지되고 불러온 30건/표시 5건이 됐다. 원문 조건 `B000` / `031||전체,`를 확인했다.
- 필터 초기화 후 동·읍·면으로 돌아가면 당근이 활성화됐다. 이의동 검색에서 알바몬 → 당근 → 알바천국 순으로 총 43건을 표시했다. 알바천국은 영통구 기준이며 동 단위 미지원 안내가 표시됐다.
- 수원시 전체를 선택한 상태에서 홍대입구역 `서울 마포구 양화로 지하 160`으로 주소를 변경하면 지역 단위가 서울 시·도 전체로 정규화됐다. 서울 전체 첫 조회는 두 업체 모두 실패했다. 같은 지역 조건의 후속 API 조회와 브라우저 재검색은 각각 20건·10건을 반환했고 원문 조건 `I000` / `02||전체,`가 적용됐다. 실패의 정확한 원인은 확정하지 않았으며, 재검색 성공을 외부 연결의 상시 성공 보장으로 해석하지 않는다.
- 전국 선택 시 지역 단위 선택창이 비활성화됐고 재검색에서 두 업체 30건을 표시했다. 원문 링크에 이전 지역 조건이 남지 않았다.
- Chrome에서 수집한 오류 수준 콘솔 로그는 0건이었다. 데스크톱 UI로 검증했으며 모바일 실기기 검증은 수행하지 않았다.

위 Chrome 검증은 기존 내장 브라우저 새 탭의 주소 검색 프레임 차단 문제 해결을 의미하지 않는다.

### 당근 여러 동네 선택 검증 — 2026-09-23

- `check` 오류·경고 0건, `test:unit` **70개 통과**, `build`, `test:search`, 새 `test:multi`를 통과했다. 추가된 13개 단위 테스트는 행정 필드만 복사, 읍·면의 서로 다른 리 중복 제거, 최대 5곳, 원선택순 합치기, ID/URL 중복 제거, HTTP·JSON·연결 실패, 일부 실패+성공 0건, 최대 동시성 2 및 취소 후 큐 중단을 포함한다.
- `test:multi`는 실제 서교동·동교동의 서로 다른 당근 `regionId=230` / `6098` 요청을 확인하고, 각 20건의 원문 결과와 합산 결과의 URL·순서를 독립 비교했다. 40건 중 중복 20건을 제거해 고유 20건을 반환했다. 최초 실행에서는 두 동네 모두 조회 실패하여 검사가 실패했다. 실패의 정확한 원인은 확정하지 않았고, 후속 같은 조건 API 확인과 검사 재실행은 성공했다. 검사는 연결 실패를 성공으로 바꾸거나 무시하지 않는다.
- Chrome 새 탭에서 공식 주소 검색으로 홍대입구역 주소를 지정한 후 여러 동네 선택을 켜면 동교동이 자동 추가됐다. 같은 기준 동네 추가 버튼은 `이미 선택한 동네예요`를 표시하고 1곳을 유지했다.
- 추가용 공식 주소 검색에서 메세나폴리스와 경기도청 주소를 선택해 서교동·이의동을 추가했다. 기준 주소는 홍대입구역, 알바몬·알바천국 범위는 마포구로 유지됐다. 당근 목록에는 동네 이름만 표시됐다.
- `카페` 실제 검색은 알바몬 20건 → 당근 40건 → 알바천국 10건, 총 70건이었다. 당근은 세 동네 각각 20건, 중복 20건 제외를 표시했고, 표시된 당근 공고 URL 40개는 모두 고유했다. 원문 링크의 지역 ID는 선택 순서대로 동교동 `6098`, 서교동 `230`, 이의동 `4538`이었다.
- 최소 시급 12,000원 필터 적용 후 당근의 표시 시급 11개가 모두 기준 이상이었다. 전국으로 바꿔 재검색해도 필터와 당근 별도 3곳이 유지됐고, 알바몬·알바천국 원문 링크에서는 지역 조건이 제거됐다. 전국은 당근의 전국 검색으로 표시하지 않았다.
- 여러 동네 모드를 끄면 전국의 당근 버튼이 비활성화됐다. 다시 켜면 3곳이 복원됐다. 이의동 삭제 후 재검색은 동교동·서교동 2곳, 중복 제외 당근 20건으로 반영됐다. 삭제 직후 이전 검색 결과는 이전 조회 범위로 유지되고, 재검색해야 새 목록이 적용됐다.
- 주소 기준으로 복귀해 기준 주소를 지우면 다른 업체가 선택된 상태에서는 주소 필수 안내가 나왔다. 당근만 선택하면 남은 별도 2곳을 정상 조회했다. 두 동네를 모두 지운 뒤 검색하면 최소 한 동네 추가 안내가 나왔다.
- Chrome 오류 수준 콘솔 로그는 0건이었다. 최대 5곳 제한과 부분 실패는 자동 테스트로 검증했고, 실제 UI 조작은 3곳까지 확인했다. 모바일 실기기 및 기존 내장 브라우저 새 탭의 주소 검색 차단 해결 검증은 포함하지 않는다.

### 주소 지도 보기 검증 — 2026-09-23

- `check` 오류·경고 0건, `test:unit` **78개 통과**, `build` 통과. 추가 8개 테스트는 도로명·지하·지번 주소, 특수문자의 URL 인코딩, 빈 값·제어문자·과장 길이·잘못된 Unicode 방어, 주소 변경·삭제, 세종 주소, 동네 링크의 상세 주소 배제를 확인한다.
- Chrome 새 탭에서 주소 미선택 시 기준 주소 지도 링크가 없는 것을 확인했다. 공식 주소 검색으로 `경기 수원시 영통구 도청로 30`을 선택한 뒤 지도 링크를 실제 클릭했다. 카카오맵 새 탭의 검색어와 주소 결과가 일치했고, 지도 위에 경기도청 주소 팝업과 위치가 표시되는 것을 화면으로 확인했다.
- 당근 여러 동네 선택을 켠 뒤 이의동의 지도 링크를 실제 클릭했다. 새 카카오맵 탭에서 `경기 수원시 영통구 이의동` 검색 결과와 지도 영역의 이의동 표기를 확인했다. 링크에는 도청로 30이나 우편번호를 넣지 않았다.
- 카페 실조회는 세 업체 불러온 50건, 최소 시급 12,000원 필터 후 표시 5건이었다. 당근 결과의 동네 지도 링크도 선택한 이의동 링크와 일치했다.
- 기준 주소를 홍대입구역 `서울 마포구 양화로 지하 160`으로 바꾸면 기준 주소 지도 링크만 새 주소로 갱신됐으며, 별도 당근 목록과 이미 조회된 결과의 지도는 이의동으로 유지됐다. 검색어와 시급 필터도 유지됐다.
- 기준 주소를 지우면 해당 지도 링크가 사라지고 별도 동네 링크는 유지됐다. 이의동을 선택 목록에서 삭제하면 목록의 지도 링크도 사라졌으나, 조회 결과는 조회 당시 동네 링크를 유지했다. 앱의 Chrome 오류 수준 콘솔 로그는 0건이었다.
- 이 검증은 데스크톱 Chrome 기준이며 모바일 실기기, 앱 안의 지도 임베드, 개별 공고 근무지 좌표 표시 또는 기존 내장 브라우저 주소 검색 차단 해결을 검증한 것은 아니다.
